export const COURSES_BY_LEVEL = {
  Bakalauras: ['1', '2', '3', '4'].map((n) => ({
    label: `Kursas ${n}`,
    value: n,
  })),
  Magistrantūra: ['1', '2'].map((n) => ({
    label: `Kursas ${n}`,
    value: n,
  })),
};
